# MongoDB Configuration
# For MongoDB Atlas (replace with your credentials):
# MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/expenseTracker

# For local MongoDB (make sure MongoDB is installed and running):
MONGODB_URI=mongodb://localhost:27017/expenseTracker

# JWT Configuration
ACCESS_TOKEN_SECRET=your-access-token-secret-key-here
REFRESH_TOKEN_SECRET=your-refresh-token-secret-key-here
ACCESS_TOKEN_EXPIRY=1d
REFRESH_TOKEN_EXPIRY=10d

# CORS Configuration
CORS_ORIGIN=http://localhost:4200

# Port Configuration
PORT=3003

# Environment
NODE_ENV=development